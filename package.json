{
  "name": "statement-fetch",
  "private": true,
  "description": "No runtime JS of its own. The driver shells out to the globally installed `agent-browser` CLI; nbb runs the ClojureScript directly.",
  "scripts": {
    "test": "kbb --backend sci --classpath src:test:resources test/run_tests.cljk"
  }
}
